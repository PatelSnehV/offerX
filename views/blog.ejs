
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog - Discount Hunter</title>
    <style>
        :root {
            --primary: #6D28D9;
            --secondary: #4C1D95;
            --background: #0F172A;
            --text: #E2E8F0;
            --accent: #8B5CF6;
            --card-bg: #1E293B;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: system-ui, -apple-system, sans-serif;
        }

        body {
            background-color:  #000;
            color:  #fff;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            padding-top: 10vw;

        }

        /* Header Section */
        .blog-header {
            text-align: center;
            padding: 4rem 0;
            position: relative;
            overflow: hidden;
        }

        .blog-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to top, transparent,var(--secondary)50%);
            opacity: 0.2;
        }

        .blog-header h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            background: linear-gradient(to right, var(--accent), var(--primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }


        /* Blog Grid */
        .blog-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 4rem;
        }

        .blog-card {
            background: var(--card-bg);
            border-radius: 1rem;
            overflow: hidden;
            transition: transform 0.3s ease;
            cursor: pointer;
            opacity: 1;
            transform: translateY(15px);
        }

        .blog-card:hover {
            transform: translateY(-0.2px);
        }

        .blog-content {
            padding: 1.5rem;
        }

        .blog-date {
            font-size: 0.8rem;
            color: #94A3B8;
            margin-bottom: 0.5rem;
        }

        .blog-title {
            font-size: 1.25rem;
            margin-bottom: 1rem;
            color:  #fff;
        }

        .blog-excerpt {
            color: #94A3B8;
            margin-bottom: 1rem;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .read-more {
            color: var(--accent);
            text-decoration: none;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .read-more:hover {
            color: var(--primary);
        }

        /* Newsletter Section */
        .newsletter {
            margin-top: 6rem;
            padding: 4rem 2rem;
            background: linear-gradient(to right, var(--card-bg),  #000);
            border-radius: 1rem;
            text-align: center;
            opacity: 1;
            transform: translateY(30px);
        }

        .newsletter h2 {
            font-size: 2rem;
            margin-bottom: 1rem;
        }

        .newsletter-form {
            max-width: 500px;
            margin: 2rem auto;
            display: flex;
            gap: 1rem;
        }

        .newsletter-input {
            flex: 1;
            padding: 1rem 1.5rem;
            border-radius: 2rem;
            border: none;
            background:  #000;
            color:  #fff;
        }

        .newsletter-input:focus {
            outline: none;
            box-shadow: 0 0 0 2px var(--accent);
        }

        .subscribe-btn {
            padding: 1rem 2rem;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 2rem;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .subscribe-btn:hover {
            background: var(--accent);
        }

        /* Loading Animation */
        .loading-spinner {
            display: none;
            justify-content: center;
            margin: 2rem 0;
        }
        .loader-container {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      background-color: #000;
    }
    .loader{
      height: 70px;
      width: 155px;
      /* background-color: red; */
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }
    .loader h1 {
      color: #fff;
      font-size: 3vw;
    }
        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid var(--card-bg);
            border-top: 4px solid var(--accent);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @media (max-width: 768px) {
            .blog-header h1 {
                font-size: 2.5rem;
            }

            .newsletter-form {
                flex-direction: column;
            }

            .container {
                padding: 1rem;
            }
        }
        .navbar {
      width: 93%;
      height: 4.8vw;
      margin-top: 3vw;
      position: fixed;
      z-index: 999;
      top: 2%;
      left: 50%;
      transform: translate(-50%, -50%);
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 5vw;
      background: #ffffff21;
      backdrop-filter: blur(30px);
      -webkit-backdrop-filter: blur(10px);
      padding: 20px 30px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      text-align: center;
    
      /* filter: contrast(100%); */
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 20px;
    }

    .logo img {
      width: 30px;
      margin-top: 1vw;
      height: 30px;
      margin-right: 3vw;
    }

    .logo h1 {
      font-size: 30px;
      /* margin: 10px; */
      margin-top: 1vw;
      font-weight: bold;
      color: white;
      text-align: center;
      align-items: center;
    }

    .nav-links {
      display: flex;
      justify-content: space-between;
      gap: 90px;
    }

    .nav-links a {
      text-decoration: none;
      color: white;
      font-size: 15px;
      transition: 0.3s;
    }

    .nav-links a:hover {
      color: #999;
    }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="logo">
          <h1>OfferX</h1>
        </div>
        <div class="nav-links">
          <a href="http://localhost:3000/dashboard">HOME</a>
          <a href="http://localhost:3000/blog">BLOG</a>
          <a href="http://localhost:3000/FAQS">FAQS</a>
          <a href="http://localhost:3000/about">ABOUT US</a>
        </div>
       
      </nav>
      <div class="loader-container">
        <div class="loader">
          <h1>OfferX</h1>
        </div>
      </div>
    <header class="blog-header">
        <div class="container">
            <h1>Latest Deals & Insights</h1>
            <p>Discover money-saving tips, tricks, and exclusive deals</p>
        </div>
    </header>

    <main class="container">

        <div class="blog-grid">
            <!-- Blog Card 1 -->
            <article class="blog-card">
                
                <div class="blog-content">
                    <div class="blog-date">February 10, 2025</div>
                    <h2 class="blog-title">10 Secret Amazon Deals You Can't Miss This Week</h2>
                    <p class="blog-excerpt">Discover hidden gems and massive discounts on Amazon that most shoppers don't know about. We've curated the best deals just for you.</p>
                    <a href="#" class="read-more">Read More →</a>
                </div>
            </article>

            <!-- Blog Card 2 -->
            <article class="blog-card">
                
                <div class="blog-content">
                    <div class="blog-date">February 9, 2025</div>
                    <h2 class="blog-title">How to Save 50% on Your Next Flight Booking</h2>
                    <p class="blog-excerpt">Learn the insider tricks that travel experts use to score amazing deals on flights. These tips could save you hundreds on your next trip.</p>
                    <a href="#" class="read-more">Read More →</a>
                </div>
            </article>

            <!-- Blog Card 3 -->
            <article class="blog-card">
                
                <div class="blog-content">
                    <div class="blog-date">February 8, 2025</div>
                    <h2 class="blog-title">Best Tech Deals: February Edition</h2>
                    <p class="blog-excerpt">From smartphones to laptops, we've rounded up the most impressive tech deals this month. Don't miss these limited-time offers!</p>
                    <a href="#" class="read-more">Read More →</a>
                </div>
            </article>
            
        </div>

        <div class="loading-spinner">
            <div class="spinner"></div>
        </div>

        <section class="newsletter">
            <h2>Never Miss a Deal</h2>
            <p>Subscribe to our newsletter and get the best deals delivered to your inbox.</p>
            <form class="newsletter-form">
                <input type="email" class="newsletter-input" placeholder="Enter your email">
                <button type="submit" class="subscribe-btn">Subscribe</button>
            </form>
        </section>
    </main>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script>
         let tl = gsap.timeline(); 
tl.from(".loader h1", {
  y: 100, // Start below
  opacity: 0,
  duration: 1.8,
  ease: "power4.out", // Smooth entry
})
tl.to(".loader h1", {
  y: -100, // Move up
  duration: 1.5,
  opacity:0,
  ease: "bounce.out", // Bounces when reaching top
});

setTimeout(() => {
            gsap.to(".loader-container", {
                opacity: 0,
                duration: 0.8,
                onComplete: () => {
                    document.querySelector(".loader-container").style.display = "none";
                    document.querySelector(".main-content").style.display = "block";
                }
            });
        }, 3000);
        // Newsletter form submission
        const newsletterForm = document.querySelector('.newsletter-form');
        newsletterForm.addEventListener('submit', (e) => {
            e.preventDefault();
            const email = newsletterForm.querySelector('input').value;
            
            if (email) {
                // Simulate successful subscription
                const btn = newsletterForm.querySelector('button');
                const originalText = btn.textContent;
                btn.textContent = 'Subscribed!';
                btn.style.background = '#10B981';
                
                setTimeout(() => {
                    btn.textContent = originalText;
                    btn.style.background = '';
                    newsletterForm.reset();
                }, 2000);
            }
        });
    </script>
</body>
</html>
